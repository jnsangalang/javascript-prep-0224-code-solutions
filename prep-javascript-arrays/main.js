const language = ['html', 'css', 'javascript', 'react', 'node', 'postgres'];

console.log(language);

const firstElement = language[0];
console.log('The first element is:', firstElement);

const thirdElement = language[2];
console.log('The third element is:', thirdElement);

const length = language.length;
console.log('The length of this array is:', length);

const lastElement = language[language.length - 1];
console.log('The last element is:', lastElement);
